services:
  songo-web:
    build:
      context: .
    container_name: songo_web
    restart: unless-stopped
    expose:
      - '80'
    networks:
      - default

networks:
  default:
    external: true
    name: npm_proxy
